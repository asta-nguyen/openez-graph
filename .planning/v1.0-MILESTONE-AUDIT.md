---
milestone: v1.0
audited: 2026-06-28T15:00:00.000Z
status: tech_debt
scores:
  requirements: 46/48
  phases: 10/10
  integration: 6/6
  flows: 6/6
gaps:
  requirements:
    - id: "QHL-02"
      status: "partial"
      phase: "Phase 8: Query History"
      claimed_by_plans: ["08-01", "08-02", "08-03", "08-04", "08-05"]
      completed_by_plans: ["08-01", "08-02", "08-03", "08-04", "08-05"]
      verification_status: "missing"
      evidence: "API supports fromTime/toTime filtering (index.ts:854-855) and queryLogsQueryOptions accepts time range params, but no date picker or time range UI controls exist in query.tsx. Sorting by latency is implemented."
    - id: "GRF-05"
      status: "partial"
      phase: "Phase 9: Graph Explorer Filtering"
      claimed_by_plans: ["09-01", "09-02", "09-03", "09-04", "09-05", "09-06"]
      completed_by_plans: ["09-01", "09-02", "09-03", "09-04", "09-05", "09-06"]
      verification_status: "missing"
      evidence: "three-forcegraph@1.43.4 is in package.json deps but NOT imported in WorkspaceGraph.tsx. Implementation uses raw Three.js (Scene, OrbitControls, sprite visibility toggling). The intent (avoid react-force-graph-3d which owns its own scene, filter without rebuild) is achieved via manual sprite.visible toggling, but the specific library API (nodeVisibility/nodeColor/zoomToFit(nodeFilterFn)) is not used."
  integration:
    - from: "Phase 1 (Workspace Selector)"
      to: "All downstream pages"
      issue: "workspace-selector.tsx:55 has fallback workspaces[0]?.id — minor WS-01 concern"
      severity: "LOW"
      requirement_ids: ["WS-01"]
      evidence: "Root beforeLoad already resolves default workspace by most-recently-indexed, so this fallback is unlikely to be hit"
  flows: []
tech_debt:
  - phase: "08-query-history"
    items:
      - "QHL-02: Time range filtering UI controls missing (API supports it)"
  - phase: "09-graph-explorer-filtering"
    items:
      - "GRF-05: three-forcegraph installed but not imported; raw Three.js used instead"
  - phase: "01-workspace-selector-foundation"
    items:
      - "workspace-selector.tsx:55 has workspaces[0]?.id fallback (mitigated by root beforeLoad)"
  - phase: "all"
    items:
      - "No VERIFICATION.md files exist for any phase — verification loop was never run"
      - "REQUIREMENTS.md checkboxes all unchecked despite work being done"
      - "No SUMMARY.md frontmatter has requirements_completed field"
---

# Milestone Audit: v1.0 — OpenEZ Graph Web UI Completion

**Audited:** 2026-06-28
**Status:** `tech_debt` — no critical blockers, 2 partial requirements, accumulated tech debt needs review

## Scores

| Category | Score |
|----------|-------|
| Requirements | 46/48 (2 partial) |
| Phases | 10/10 (all implemented) |
| Integration | 6/6 (all E2E flows verified) |
| Flows | 6/6 (no broken flows) |

## Requirements Coverage

### 3-Source Cross-Reference

| Source | Available | Result |
|--------|-----------|--------|
| VERIFICATION.md | 0/10 phases | All missing — verification never run |
| SUMMARY.md frontmatter | 0/52 plans | No `requirements_completed` field |
| REQUIREMENTS.md checkboxes | 0/48 checked | All unchecked despite work done |

**Automated status:** All 48 requirements would be `unsatisfied` per the status matrix (missing VERIFICATION + missing frontmatter + unchecked). However, **manual codebase verification** confirms 46/48 are satisfied in the code.

### Requirements by Category

#### Workspace Foundation (WS-01..05) — 5/5 SATISFIED ✓
- WS-01: Dashboard accepts workspaceId param, no all[0] hardcoding ✓
- WS-02: Documents page uses workspaceId from root search ✓
- WS-03: Workspace selector uses dropdown (not text input) ✓
- WS-04: Switching workspaces updates via URL search params + query key scoping ✓
- WS-05: All TanStack Query keys include workspaceId (verified across all 8 query option factories) ✓

#### Workspace Actions (ACT-01..04) — 4/4 SATISFIED ✓
- ACT-01: POST /api/workspaces/:id/index calls indexWorkspace ✓
- ACT-02: Reindex button microstates present ✓
- ACT-03: DELETE /api/workspaces/:id with confirmation dialog ✓
- ACT-04: DELETE /api/workspaces/:id/jobs/:jobId calls cancelIndexRun ✓

#### Real-Time Indexing Status (RTI-01..05) — 5/5 SATISFIED ✓
- RTI-01: SSE endpoint via streamSSE (index.ts:444) ✓
- RTI-02: EventSource consumption via use-sse.ts hook ✓
- RTI-03: Monotonic progress enforced in use-sse.ts; aria-live="polite" ✓
- RTI-04: Cleanup on unmount/done/error in use-sse.ts ✓
- RTI-05: Error event handling with inline destructive message ✓

#### Documents (DOC-01..05) — 5/5 SATISFIED ✓
- DOC-01: Documents page uses workspaceId ✓
- DOC-02: Table columns: path, language/kind, size, chunk count, symbol count, last-indexed ✓
- DOC-03: Filtering by path substring, language/kind, file type ✓
- DOC-04: Server-side pagination + sorting via manualPagination ✓
- DOC-05: Row click navigates to chunk viewer ✓

#### Chunk Viewer (CHK-01..06) — 6/6 SATISFIED ✓
- CHK-01: Route at workspaces/$workspaceId/documents/$documentId/chunks ✓
- CHK-02: List shows index, line range, token count, char count, overlap ✓
- CHK-03: Syntax highlighting via Shiki (chunk-content.tsx) ✓
- CHK-04: Boundary indicators via chunk-boundary-bar.tsx ✓
- CHK-05: StructuredContentViewer for YAML/JSON/TOML tree/text toggle ✓
- CHK-06: Read-only (no edit UI) ✓

#### Symbol Browser (SYM-01..04) — 4/4 SATISFIED ✓
- SYM-01: Symbols grouped by type (function, class, interface, variable, method) ✓
- SYM-02: Search by name via searchGraphNodesByLabel ✓
- SYM-03: Rows show name, type, file path, line number, signature ✓
- SYM-04: Click navigates to chunk viewer via refId ✓

#### Graph Explorer (GRF-01..06) — 5/6 SATISFIED, 1 PARTIAL
- GRF-01: Filter by node type ✓
- GRF-02: Search by label ✓
- GRF-03: Degree filtering (min/max slider) ✓
- GRF-04: Filter toggles sprite.visible without scene rebuild ✓
- GRF-05: **PARTIAL** — three-forcegraph@1.43.4 installed but not imported; raw Three.js used
- GRF-06: Click → detail panel, double-click → navigation ✓

#### Memories (MEM-01..05) — 5/5 SATISFIED ✓
- MEM-01: Memories table (id, type, source, timestamps, content preview) ✓
- MEM-02: GET /api/workspaces/:id/memories endpoint ✓
- MEM-03: Detail panel with full content ✓
- MEM-04: Dashboard recent memories shows real data ✓
- MEM-05: View-only (no create/edit/delete in UI) ✓

#### Query History (QHL-01..04) — 3/4 SATISFIED, 1 PARTIAL
- QHL-01: Query logs table (query text, timestamp, latency, result count) ✓
- QHL-02: **PARTIAL** — API supports time range filtering, but no UI date picker controls
- QHL-03: Row expand shows retrieved chunk IDs and scores ✓
- QHL-04: Read-only ✓

#### Settings (SET-01..04) — 4/4 SATISFIED ✓
- SET-01: No Redis placeholder ✓
- SET-02: settingsEnvQueryOptions removed from sidebar ✓
- SET-03: MCP setup configs present ✓
- SET-04: Subtraction only — no new features added ✓

## Cross-Phase Integration

### E2E Flows Verified (6/6)

| Flow | Phases | Status | Evidence |
|------|--------|--------|----------|
| Dashboard → Select Workspace → Documents → Chunk Viewer | 1→4→5 | ✓ VERIFIED | workspaceId flows through URL search params + query keys |
| Workspace Detail → Reindex → SSE Progress → Completion | 2→3 | ✓ VERIFIED | POST index → streamSSE → EventSource → indexing-progress.tsx |
| Symbol Browser → Click Symbol → Chunk Viewer | 6→5 | ✓ VERIFIED | symbols.tsx row click → chunks route via refId |
| Graph Explorer → Filter → Click Node → Detail Panel | 9 | ✓ VERIFIED | GraphClient filtering + inspector panel |
| Workspace ID propagation to all pages | 1→all | ✓ VERIFIED | All 8 query option factories scope by workspaceId |
| Settings cleanup (no Redis, no stale fetches) | 10 | ✓ VERIFIED | No Redis refs, no settingsEnvQueryOptions |

### Integration Gaps

| From → To | Issue | Severity | REQ IDs |
|-----------|-------|----------|---------|
| Phase 1 → All | workspace-selector.tsx:55 fallback `workspaces[0]?.id` | LOW | WS-01 |

### API Completeness

All 12 API endpoints verified present and wired to UI:
- Dashboard, Documents, Chunks, Symbols, Memories, Query Logs, Graph
- Reindex (POST), Cancel (DELETE), Delete Workspace (DELETE)
- SSE stream, Query (POST)

## Tech Debt

| Phase | Item |
|-------|------|
| 08-query-history | QHL-02: Time range filtering UI controls missing (API supports it) |
| 09-graph-explorer-filtering | GRF-05: three-forcegraph installed but not imported; raw Three.js used |
| 01-workspace-selector-foundation | workspace-selector.tsx:55 has workspaces[0]?.id fallback (mitigated) |
| all | No VERIFICATION.md files — verification loop never run |
| all | REQUIREMENTS.md checkboxes all unchecked despite work done |
| all | No SUMMARY.md frontmatter has requirements_completed field |

## Post-Implementation Work (Outside GSD Workflow)

These items were completed after the 10 phases but before this audit:
- Dither visual style applied across entire UI (globals.css)
- All lucide-react icons replaced with itshover animated icons (withAutoAnimate HOC)
- @tanstack/react-virtual integrated for chunk list virtualization
- feat/improve-zero-config merged (Python indexing + zero-config)
- FTS memory search integrated (memories_fts table + searchMemories)

## Recommendation

The milestone is **functionally complete** with 46/48 requirements satisfied and all 6 E2E flows verified. The 2 partial requirements (QHL-02, GRF-05) are non-critical tech debt:

- **QHL-02**: The API supports time range filtering; only the UI controls are missing. Can be added in a future phase.
- **GRF-05**: The filtering-without-rebuild goal is achieved via raw Three.js sprite visibility toggling. The three-forcegraph library is installed but unused. Either import it or remove the dependency.

**Recommended action:** Complete the milestone (accept tech debt) or insert a closure phase for the 2 partial requirements.
